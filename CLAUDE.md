# Instructions

- Do not create git commits automatically. Only commit when the user explicitly asks (e.g. "комить"/"commit"). After finishing edits, run the relevant build/tests, report status, and leave the working tree uncommitted. The user typically commits in batches.
